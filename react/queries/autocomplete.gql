query Autocomplete($maxRows: Int, $inputValue: String) {
  autocompleteSearchSuggestions(fullText: $inputValue)
    @context(provider: "vtex.search-graphql") {
    itemsReturned {
      thumb
      name
      href
      productId
      criteria
      slug
    }
  }
}

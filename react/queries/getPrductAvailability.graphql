query getAvbProduct($refIds: [String]) {
  getSkuAvailability(refIds: $refIds) @context(provider: "sbdsefuat.bulk-order") {
    items {
      refid
      sku
      productId
      availability
      sellers {
        id
        name
      }
      availableQuantity
      productName
      price
    }
  }
}
